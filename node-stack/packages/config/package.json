{
  "name": "@node-stack/config",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "build": "tsc -b --clean && tsc -b"
  },
  "dependencies": {
    "@nestjs/common": "latest",
    "@nestjs/config": "latest",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "latest"
  }
}